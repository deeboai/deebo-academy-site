# Source Directory

## Purpose

This folder contains the application source code that supports the routes in `app`.

## Major Areas

- `actions`: server actions
- `components`: reusable interface pieces
- `content`: structured marketing content
- `curriculum`: file-based curriculum lessons
- `hooks`: shared client hooks
- `lib`: server helpers, data access, auth, email, and integrations
