# Academy Workflow

The current workflow is intentionally narrow:

1. A family submits intake through `/book`.
2. An admin signs in through `/login`.
3. The admin reviews submissions in `/admin/intake`.
4. The admin updates intake status, records notes, and uses the detail page as the source of truth for follow-up.

The repository no longer includes parent, student, or tutor portals.
