# App Directory

This folder contains the public Academy site plus the admin-only intake workspace.

## Active Route Areas

- `admin`: intake review pages
- `api`: server endpoints such as Stripe webhook handling
- `book`: public intake flow
- `login`: admin sign-in
- public marketing routes such as `subjects`, `pricing`, `faq`, `privacy`, and `terms`
